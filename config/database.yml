defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: edwardanderson
  host: localhost


development:
  <<: *defaults
  database: omni_development

test:
  <<: *defaults
  database: omni_test
